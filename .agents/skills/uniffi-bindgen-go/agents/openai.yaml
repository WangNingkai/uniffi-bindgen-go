interface:
  display_name: "Uniffi Bindgen Go"
  short_description: "Repo-specific patterns and workflows for uniffi-bindgen-go"
  default_prompt: "Use the uniffi-bindgen-go repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true